spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test1 # DB ??, ?? ??? ?? mydb ??? ??
    username: root # DB ????
    password: 1234 # DB ????
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none # (create, update, validate, none ? ??)
    show-sql: true # ???? SQL? ??